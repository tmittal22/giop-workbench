__pycache__/
*.pyc
build/
dist/
*.egg-info/
.pytest_cache/
figures/*.png

# The package data directory is POPULATED, not authored: scripts/fetch_data.py copies
# the tables out of the vendored upstream_matlab/, or downloads them if it is absent.
# Kept out of git so the same bytes do not sit in two places and drift apart.
src/giop/data/*.txt
src/giop/data/*.mat
src/giop/data/*.dat
